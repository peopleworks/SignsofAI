<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <RootNamespace>SignsOfAI.Calibration</RootNamespace>

    <!-- A measuring instrument, not something anyone should install. It exists to produce
         Docs/CALIBRATION.md, which is the thing that ships. -->
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\SignsOfAI.Core\SignsOfAI.Core.csproj" />
  </ItemGroup>

</Project>
